# Must be the name of the file, but in camelCase and extend ApplicationController
class AboutController < ApplicationController
	def index
		# If this renders anything, use it, otherwise use default ApplicationController renderer
	end
end